public class Todo {
//    String fileLocation = Strings.EMPTY ;
//        try {
//        fileLocation = args[0];
//    }
//        catch (final Exception ex) {
//        if(!Strings.hasText(fileLocation)) {
//            throw new RuntimeException("The first argument must be the file location of car evaluation dataset!");
//        }
//    }
//        System.out.println(fileLocation);

}
